// test/converter.js

var expect    = require("chai").expect;
const dashboard = require('./dashboard');

describe('#getFoo', () => {
  it('returns foo', async () => {
    let ret = await dashboard.getDayRisk()
    
  })
})